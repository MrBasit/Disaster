"use strict";

function determineAid(disaster, aid) {
  return disaster * aid * 100;
}

function determineCurrency(disaster) {
  return parseInt(disaster) * getCountry.length * 200;
}
// Add additional functions below
